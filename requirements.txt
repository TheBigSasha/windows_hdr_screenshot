# Dependencies are declared in pyproject.toml — the single source of truth.
# This file just installs the package (with the GUI extra) from that spec, so
# `pip install -r requirements.txt` keeps working without duplicating versions.
-e .[gui]

# Headless / agent install (no Qt):   pip install -e .
# Optional extras:
#   .[heic]      10-bit PQ HEIC output (pulls x265/GPL — see THIRD_PARTY_NOTICES.md)
#   .[avif-hdr]  true 10-bit PQ HDR AVIF via imagecodecs
#   .[all]       gui + heic + avif-hdr
#   .[dev]       everything + pytest/ruff/pyright
